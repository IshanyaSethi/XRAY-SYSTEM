fastapi==0.104.1
uvicorn[standard]==0.24.0
# PostgreSQL adapter (optional - only needed if using PostgreSQL storage)
# Uncomment the line below if you need PostgreSQL support
# psycopg2-binary==2.9.9
python-dotenv==1.0.0

